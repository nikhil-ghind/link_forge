# Origin the dashboard calls for the API. Leave blank in production, where
# Apache serves the dashboard and /api from the same vhost.
VITE_API_ORIGIN=http://127.0.0.1:8000

# API token issued with `php artisan linkforge:token dashboard`.
# Stored in localStorage after the first entry; this is only the dev default.
VITE_API_TOKEN=
